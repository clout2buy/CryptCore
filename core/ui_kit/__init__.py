"""Pure UI render kit for the terminal facade.

The public app still imports ``core.ui``. This package holds the reusable,
state-light render pieces so the facade can stay small and compatibility-safe.
"""

